#include "Cavalry.h"

using namespace std;

void Cavalry::charge(Unite *enemy) {
    /*
    cout << name << " charges " << enemy->name << ". ";
    int damage = fight(0);
    damage=enemy->fight(2*damage);
    fight(damage);
    cout << endl;
     */
}