#include "Fusiliery.h"

using namespace std;

void Fusiliery::shoot(Unite* enemy){
    /*
    cout << name << " shoots " << enemy->name << ". ";
    int damage = firstWeapon->attack(squadSize);
    damage=enemy->fight(damage);
    cout << endl;
    */
}