#include "Infantry.h"

using namespace std;

void Infantry::protect(Unite *enemy) {
    /*
    cout << name << " attacks " << enemy->name << ". ";
    int damage = fight(0);
    damage=enemy->fight(damage);
    fight(damage/2);
    cout << endl;
    */
}